datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Alojamiento {
  id           Int    @id @default(autoincrement())
  tipo         String
  titulo       String
  calificacion String
  ubicacion    String
  precio       String
  imagen       String
  url_airbnb   String
}